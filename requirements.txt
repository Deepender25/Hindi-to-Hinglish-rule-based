# Hindi to Hinglish Converter
# A Python project that converts Hindi text to natural conversational Hinglish

# Core dependencies (rule-based only needs this)
indic-transliteration>=2.3.0

# Gemini Hybrid Converter dependencies (optional)
google-genai>=1.0.0
python-dotenv>=1.0.0

# Legacy ML dependencies (not needed for rule-based or Gemini)
# pandas>=1.5.0
# torch>=2.0.0
# transformers>=4.35.0
# accelerate>=0.20.0
# sentencepiece>=0.1.99
# protobuf>=3.20.0

# Optional dependencies for development
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0
